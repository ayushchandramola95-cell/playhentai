# 📺 Watch / Video Player Page Deep Architectural & SEO Audit (`structur&seo/watch_page.md`)

This document presents an exhaustive, 8-pillar structural, technical, data, SEO, and performance analysis of the **Watch / Video Player Page** (`/watch/[episodeId]`).

---

## Pillar 1: 📌 Page Overview & Routing Architecture

- **Route URL Pattern**: `/watch/[episodeId]` (Canonical URL e.g. `https://playhentai.live/watch/[series-slug]-episode-[epNum]`).
- **File System Locations**:
  - **Main Page Component**: `src/app/(public)/watch/[episodeId]/page.tsx`
  - **Client Page Component**: `src/app/(public)/watch/[episodeId]/WatchPageClient.tsx`
  - **Supporting Sub-Components**: `VideoPlayer.tsx`, `CommentSection.tsx`, `SimilarTitles.tsx`, `AdBanner.tsx`
  - **URL Utility Resolvers**: `src/utils/episodeUrl.ts`
  - **CSS Modules**: `src/app/(public)/watch/[episodeId]/watch.module.css`
  - **Public Layout Wrapper**: `src/app/(public)/layout.tsx`
- **Next.js App Router Rendering Strategy**:
  - Server Component (`page.tsx`) resolves episode, series, and season details from Supabase and passes initial props to interactive Client Sub-Components (`'use client'`).
  - Currently set to `export const dynamic = 'force-dynamic'` (Needs optimization: remove dynamic flag and wrap query in 60-second `unstable_cache` with singleton public Supabase client reuse).
- **Access Control & Auth Guards**:
  - **Public Access**: 100% accessible to all users.

---

## Pillar 2: 🧩 Visual & UI Component Structure

### DOM & Section Hierarchy Tree:
```
RootLayout (src/app/layout.tsx)
 └── PublicLayout (src/app/(public)/layout.tsx)
      ├── Header (src/components/Header/Header.tsx)
      ├── <main> (Flex Column, paddingTop: 64px)
      │    └── WatchPage (src/app/(public)/watch/[episodeId]/page.tsx)
      │         ├── JsonLd (VideoObject + BreadcrumbList Schemas)
      │         └── WatchPageClient (src/app/(public)/watch/[episodeId]/WatchPageClient.tsx)
      │              ├── Visible Breadcrumb Links (.breadcrumbs) [Home > Series > Show Title > Episode Num]
      │              └── Player Layout Grid (.playerLayout)
      │                   ├── Left Column (.playerCol)
      │                   │    ├── Video Player Wrapper (.videoWrapper) [VideoPlayer.tsx]
      │                   │    ├── Server & Ep Navigation Bar (.serverRow)
      │                   │    │    ├── Server Selector Chips (.serverChips) [Server 1]
      │                   │    │    └── Action Buttons (Prev Ep + Next Ep + Favorite)
      │                   │    ├── Episode Info Card (.episodeInfoCard)
      │                   │    │    ├── Episode Number Badge + <h1>Watch {seriesTitle} Episode {epNum}: {epTitle}</h1>
      │                   │    │    ├── Aired Date, Duration, 1080p Ultra HD Badge
      │                   │    │    └── Episode Synopsis Paragraph (.epDescription)
      │                   │    ├── Mobile-Only Series Queue Block (.mobileOnlySeriesQueueBlock)
      │                   │    └── Community Discussion Comments Section (CommentSection.tsx)
      │                   └── Right Sidebar Column (.sidebarCol)
      │                        ├── Series Quick Info Card (.sidebarCard) [Poster + Title + View Series Link]
      │                        ├── Episodes Queue Box (.episodesQueueCard) [Scrollable episode list]
      │                        ├── AdBanner (Zone 5986838)
      │                        └── Similar Titles Recommendations Carousel (SimilarTitles.tsx)
      ├── AdBanner [Desktop Footer] (Zone 5986212)
      ├── AdBanner [Mobile Footer] (Zone 5986980)
      └── Footer (src/components/Footer/Footer.tsx)
```

### Key Component Composition & Props:
1. `<VideoPlayer videoUrl={videoUrl} title={epTitle} episodeNumber={epNum} nextEpisodeUrl={nextEpUrl} />`: Custom HTML5 video player component with theatre mode toggle, auto-play next episode, and volume/fullscreen controls.
2. Server Selector & Navigation Row:
   - Fast episode navigation (`Prev Ep` / `Next Ep` buttons) and Watchlist/Favorite toggle.
3. Episodes Queue Box:
   - Scrollable sidebar list featuring all episodes in the season, highlighting the currently playing episode with active styling and auto-scrolling into view.

---

## Pillar 3: ⚡ Data Fetching, Cache & State Architecture

### Database Queries & Episode Resolver:
```typescript
const getCachedResolvedEpisode = unstable_cache(
  async (episodeId: string) => {
    return await resolveEpisode(publicSupabaseClient, episodeId);
  },
  ['watch-episode-cache-v1'],
  { revalidate: 60, tags: ['watch_episode'] }
);
```

---

## Pillar 4: 🔍 SEO & Search Engine Indexability Audit

### Metadata Source & Generator:
- **Metadata Generated By**: `src/app/(public)/watch/[episodeId]/page.tsx` (`generateMetadata({ params })`)
- **Metadata Type**: `Dynamic Metadata` function

### Search Intent & SEO Goal:
- **Target User Intent**: Streaming and watching full anime episodes (`watch [Series Title] episode [num]`).
- **Detailed Intent Breakdown**:
  - Streaming full video episode in 1080p HD
  - Navigating to previous/next episode
  - Browsing season episode queue
  - Reading episode summary synopsis
  - Leaving comments & rating episode
- **SEO Goal**: Rank #1 on Google Video Search and SERPs for episode streaming queries (`watch [Series Title] episode [num] english sub hd`).

### Target Keyword Strategy & Keyword Mapping:
- **Primary Keyword**: `watch [Series Title] episode [num] english sub`
- **Supporting / Secondary Keywords**:
  - `[Series Title] ep [num] uncensored`
  - `[Series Title] episode [num] 1080p`
  - `[Series Title] episode [num] stream`
  - `[Series Title] episode [num] online`
- **Primary Terms Present**:
  - `Watch` (Present in Title Tag, Meta Description, H1, Breadcrumbs, CTAs)
  - `Series Title` (Present in Title Tag, Meta Description, H1, Breadcrumbs, JSON-LD)
  - `Episode Number` (Present in Title Tag, Meta Description, H1, Breadcrumbs, JSON-LD)
  - `English Sub HD` (Present in Title Tag & Meta Description)
  - `Brand Name` (`PlayHentai` present in Title Tag & Meta Description)

### Technical Crawlability Matrix:
- **Included in Sitemap**: `Yes` (via `/sitemap.ts` listing all episode watch URLs)
- **Self Canonical**: `Yes` (`https://playhentai.live/watch/[series-slug]-episode-[epNum]`) - *Dynamic per episode watch URL*
- **Noindex**: `No` (`index: true, follow: true`)
- **Robots Directives**: `index: true`, `follow: true`, `googleBot: { index: true, follow: true, max-image-preview: 'large', max-video-preview: -1, max-snippet: -1 }` (Inherited from Root Layout)
- **Pagination**: `None`

### Dynamic Metadata Inspection:
- **Title Tag**: `Watch ${seriesTitle} Episode ${ep.episode_number} English Sub HD | PlayHentai`
- **Meta Description**: `Watch ${seriesTitle} Episode ${ep.episode_number} English Sub HD online on PlayHentai. Stream the latest episodes in high definition.`
- **Canonical URL**: `https://playhentai.live/watch/[series-slug]-episode-[epNum]` (Canonicalized via `getEpisodeWatchUrl`)
- **OpenGraph Metadata**: `og:title`, `og:description`, `og:url`, `og:type` (`video.episode`), `og:image`.
- **OpenGraph Image (`og:image`)**: Episode thumbnail image delivered via Cloudflare R2 CDN (`https://media.playhentai.live`). Default site banner fallback (`https://media.playhentai.live/og-banner.jpg`) applied if thumbnail is missing.

### Semantic HTML & DOM H1 Verification:
- **`<h1>` Verification**:
  - *Current H1*: `<h1>Watch {seriesTitle} Episode {activeEpisode.episode_number}: {activeEpisode.title}</h1>` in `WatchPageClient.tsx`.
  - *Result*: **Passed (100% Single H1 HTML DOM)**. Aligned with target keyword `watch [Series Title] episode [num]`.
- **`<h2>` Headers**: `<h2>Episode Not Found</h2>`, `<h2>Similar Titles You Might Like</h2>`.

### JSON-LD Structured Data Implementation:
1. **Implemented Schemas**:
   - `BreadcrumbList` (`Home` $\rightarrow$ `[Series Title]` $\rightarrow$ `Episode [Num]`)
   - `VideoObject` (with `@id`, `name`, `description`, `thumbnailUrl`, `uploadDate`, `duration`, `contentUrl`, `embedUrl`, `url`, `publisher`, `partOfSeries`)
     - *Schema Data Dependencies*: Depends directly on `episodes` table (`video_key`, `thumbnail_key`, `duration_seconds`), `series` table, and `seasons` table.
2. **Not Implemented / Intentionally Omitted Schemas**:
   - `CollectionPage`: (Omitted; reserved for index/catalog pages)

### Internal Linking Map (`Watch Page Links To`):
- **Series Profile Landing Page**: `/series/[seriesSlug]`
- **Previous & Next Episode Watch Pages**: `/watch/[prevEpUrl]`, `/watch/[nextEpUrl]`
- **Studio Profile Page**: `/studios/[studioSlug]`
- **Header Navigation Links**: `/` (Home), `/categories` (Series), `/uncensored` (Uncensored), `/3d` (3D), `/playlists` (Playlists), `/studios` (Studios), `/surprise` (Surprise Me)
- **User Activity Links**: `/watchlist` (Watchlist), `/history` (Watch History)
- **Global Search**: Triggered via Header Search Bar
- **Footer Legal & Technical Links**: `/dmca`, `/privacy`, `/terms`, `/2257`, `/contact`

---

## Pillar 5: ⚡ Performance & Core Web Vitals (CWV) Audit

- **Layout Shift Prevention (CLS)**:
  - Video player container uses 16:9 aspect ratio box styling.
- **SSR Revalidation & Cache**:
  - Currently uses `export const dynamic = 'force-dynamic'`.
  - *Optimization Needed*: Remove dynamic flag and wrap episode resolver in `unstable_cache` (60s TTL) with singleton public Supabase client reuse to achieve server TTFB under 80ms.

---

## Pillar 6: 💰 Monetization, Ads & Analytics

### Ad Placement Architecture:
1. **Sidebar Ad Banner**: Zone `5986838` placed inside the right sidebar column.
2. **Pre-Footer Ad Banner**: Zone `5986212` (Desktop) & Zone `5986980` (Mobile).

---

## Pillar 7: ♿ Accessibility (a11y) & Mobile UX Hygiene

- **Auto-Scroll Behavior**: `useEffect` instantly scrolls browser window to top on episode change and auto-scrolls playing item inside sidebar queue.
- **Interactive Controls**: Touch target buttons for Prev Ep, Next Ep, Server Selector, and Favorite buttons meet 48px minimum standard.

---

## Pillar 8: 🛠️ Actionable Improvements & Execution Status

### 🚀 High-Priority SEO Improvements:
1. **H1 Target Keyword Alignment**:
   - **Status**: ✅ **COMPLETED**.
   - *Implementation*: Updated H1 tag to `<h1>Watch {seriesTitle} Episode {activeEpisode.episode_number}: {activeEpisode.title}</h1>` in `src/app/(public)/watch/[episodeId]/WatchPageClient.tsx` for 100% search keyword alignment (`watch [Series Title] episode [num]`).
2. **`VideoObject` Structured Data Implementation**:
   - **Status**: ✅ **COMPLETED**.
   - *Implementation*: Verified dynamic `VideoObject` JSON-LD schema injection in `src/app/(public)/watch/[episodeId]/page.tsx` including `thumbnailUrl`, `contentUrl` (direct R2 MP4 URL), `embedUrl`, `duration`, and `partOfSeries` (`TVSeries`) for Google Video Rich Snippet eligibility.
3. **OpenGraph Image (`og:image`) & Social Sharing Banners**:
   - **Status**: ✅ **COMPLETED**.
   - *Implementation*: Implemented `generateMetadata({ params })` in `src/app/(public)/watch/[episodeId]/page.tsx` adding default site poster banner URL (`https://media.playhentai.live/og-banner.jpg`) as fallback when episode thumbnail is missing, alongside OpenGraph and Twitter Card metadata.

### ⚡ Performance & CWV Enhancements:
1. **Remove `force-dynamic` & Cache Episode Resolver Queries for 60 Seconds (`unstable_cache`) & Client Reuse**:
   - **Status**: ✅ **COMPLETED**.
   - *Implementation*: Removed `export const dynamic = 'force-dynamic'` and wrapped episode resolver queries in `unstable_cache` with a 60-second TTL (`watch_episode` tag) in `page.tsx` using a singleton public Supabase client instance, bringing server TTFB response times under 80ms.

